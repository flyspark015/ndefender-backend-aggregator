# UI Contract Stubs

This folder provides a minimal typed client surface for wiring the UI to the
aggregator API when a UI repo is not available locally.

- Base REST: `/api/v1`
- WS: `/api/v1/ws`

Use `client.ts` as a starting point and replace with real UI code when the UI
repository is identified.
